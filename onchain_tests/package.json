{
  "license": "ISC",
  "scripts": {
    "test:sandbox": "near-workspaces-ava --config ./ava.config.cjs --verbose",
    "test:testnet": "near-workspaces-ava --config ./ava.testnet.config.cjs --verbose",
    "test": "yarn test:sandbox && yarn test:testnet"
  },
  "devDependencies": {
    "near-workspaces-ava": "^1.0.0"
  }
}