{
  "license": "ISC",
  "scripts": {
    "arch": "bash arch.sh",
    "test:sandbox": "yarn arch && near-workspaces-ava --config ./ava.config.cjs --verbose --timeout=10m",
    "test:testnet": "yarn arch && near-workspaces-ava --config ./ava.testnet.config.cjs --verbose --timeout=10m",
    "test": "yarn test:sandbox && yarn test:testnet"
  },
  "devDependencies": {
    "near-workspaces-ava": "1.0.0",
    "ts-node": "^10.7.0"
  }
}
