{
  "license": "ISC",
  "scripts": {
    "test:sandbox": "bash arch.sh && near-workspaces-ava --config ./ava.config.cjs --verbose",
    "test:testnet": "bash arch.sh && near-workspaces-ava --config ./ava.testnet.config.cjs --verbose",
    "test": "yarn test:sandbox && yarn test:testnet"
  },
  "devDependencies": {
    "near-workspaces-ava": "1.0.0",
    "ts-node": "^10.7.0"
  }
}