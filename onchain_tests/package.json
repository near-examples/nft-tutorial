{
  "license": "ISC",
  "scripts": {
    "serialize": "cp ./.fix-testnet/index.js ./node_modules/near-workspaces-ava/dist/",
    "test:sandbox": "near-workspaces-ava --config ./ava.config.cjs --verbose --timeout=30m",
    "test:testnet": "yarn serialize && near-workspaces-ava --config ./ava.testnet.config.cjs --verbose --timeout=30m",
    "test": "yarn test:sandbox && yarn test:testnet"
  },
  "devDependencies": {
    "near-workspaces-ava": "1.0.0",
    "ts-node": "^10.7.0"
  }
}
