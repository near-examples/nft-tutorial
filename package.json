{
  "name": "nft-tutorial",
  "version": "1.0.0",
  "description": "Zero to Hero NFT Tutorial",
  "author": "Ben Kurrek",
  "license": "ISC",
  "scripts": {
    "arch": "bash ./onchain_tests/arch.sh",
    "test:sim:sandbox": "cd onchain_tests && yarn install && yarn test:sandbox && cd ..",
    "test:sim:testnet": "cd onchain_tests && yarn install && yarn test:testnet && cd ..",
    "test:nft": "cd nft-contract && cargo test --color always && cd ..",
    "test:market": "cd market-contract && cargo test --color always && cd ..",
    "test": "yarn test:nft && yarn test:market && yarn test:sim:sandbox",
    "build:nft": "cd nft-contract && bash build.sh && cd ..",
    "build:market": "cd market-contract && bash build.sh && cd ..",
    "build": "yarn build:nft && yarn build:market"
  }
}
